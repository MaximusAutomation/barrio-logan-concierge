# Normalize all text files to LF on checkout (keeps the repo clean in
# cross-platform environments — WSL + Windows Git + Vercel Linux all agree).
* text=auto eol=lf

# Binary files — never apply line-ending conversion.
*.png  binary
*.jpg  binary
*.jpeg binary
*.webp binary
*.gif  binary
*.ico  binary
